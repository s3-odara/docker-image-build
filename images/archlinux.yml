image:
  distribution: archlinux
  description: Arch Linux Custom Image
  architecture: x86_64

source:
  downloader: archlinux-http
  url: https://archive.archlinux.org/iso
  keys:
    - 4AA4767BBC9C4B1D18AE28B77F2D434B9741E8AC

targets:
  lxc:
    create-message: |
      You just created an Arch Linux container (release={{ image.release }}, serial={{ image.serial }})
    config:
      - type: all
        content: |-
          lxc.include = LXC_TEMPLATE_CONFIG/common.conf
          lxc.include = LXC_TEMPLATE_CONFIG/userns.conf
          lxc.arch = x86_64

files:
  - name: network
    path: /etc/systemd/network/eth0.network
    content: |-
      [Match]
      Name=eth0

      [Network]
      DHCP=yes

packages:
  manager: pacman
  update: true
  cleanup: true
  
  sets:
    - packages:
        - base
		- linux
      action: install

files:
  - generator: copy
    source: ./scripts/setup.sh
    path: /tmp/setup.sh
    mode: 0755

actions:
  - trigger: post-unpack
    action: |-
      pacman-key --init
	  pacman-key --populate archlinux
      pacman-key --refresh-keys
	  pacman -Sy --noconfirm archlinux-keyring
  - trigger: post-packages
    action: |-
      /tmp/setup.sh
      rm /tmp/setup.sh

